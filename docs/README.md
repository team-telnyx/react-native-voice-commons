# API Documentation

This directory contains the auto-generated API documentation for the `@telnyx/react-voice-commons-sdk` package.

## 📚 Documentation Structure

The documentation is generated using [TypeDoc](https://typedoc.org/) from the TypeScript source code in the `react-voice-commons-sdk/src` directory.

### Contents

- **Modules**: All exported modules and their contents
- **Classes**: Main classes like `TelnyxVoipClient`, `Call`, etc.
- **Interfaces**: TypeScript interfaces for configuration and types
- **Types**: Type definitions and type aliases
- **Enums**: Enumeration types like `TelnyxConnectionState`, `TelnyxCallState`
- **Functions**: Utility and helper functions

## 🔄 Automatic Updates

Documentation is automatically regenerated on every push to the `main` or `develop` branch when source files are modified:

- **Trigger**: Push or PR to `main`/`develop` branches
- **Source**: `react-voice-commons-sdk/src/**`
- **Output**: This `docs/` directory
- **Deployment**: Automatically deployed to GitHub Pages

## 🛠️ Manual Generation

To generate documentation locally:

```bash
cd react-voice-commons-sdk
npm run docs
```

To watch for changes and regenerate automatically:

```bash
cd react-voice-commons-sdk
npm run docs:watch
```

## 📖 Viewing Documentation

### Online (GitHub Pages)
Documentation is automatically deployed to GitHub Pages and available at:
https://team-telnyx.github.io/react-native-voice-commons/

### Local
Open `docs/index.html` in your browser after generating the documentation.

## 🔧 Configuration

Documentation generation is configured in:
- `react-voice-commons-sdk/typedoc.json` - TypeDoc configuration
- `.github/workflows/docs.yml` - GitHub Actions workflow for auto-generation

## 📝 Documentation Comments

All public APIs should include TSDoc comments for proper documentation generation:

```typescript
/**
 * Creates a new VoIP client instance.
 * 
 * @param options - Configuration options for the client
 * @returns A new TelnyxVoipClient instance
 * 
 * @example
 * ```typescript
 * const client = createTelnyxVoipClient({
 *   debug: true,
 *   enableAppStateManagement: true
 * });
 * ```
 */
export function createTelnyxVoipClient(options?: TelnyxVoipClientOptions): TelnyxVoipClient {
  // ...
}
```

## 🚀 Similar Projects

This documentation setup mirrors the approach used in:
- [telnyx-webrtc-android](https://github.com/team-telnyx/telnyx-webrtc-android) (uses Dokka for Kotlin)

---

**Generated by TypeDoc** | **Maintained by Team Telnyx**
